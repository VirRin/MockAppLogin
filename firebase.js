/*// Import the functions you need from the SDKs you need
import * as firebase from "firebase";

// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyAvhP-NeEclSzSU4jWC0gjmKHh4qaY1gbM",
  authDomain: "mockapp-a580d.firebaseapp.com",
  projectId: "mockapp-a580d",
  storageBucket: "mockapp-a580d.appspot.com",
  messagingSenderId: "14341882320",
  appId: "1:14341882320:web:b51c94ba58a93e9f1379d5"
};

// Initialize Firebase
let app;
if (firebase.apps.length===0) {
  firebase.initializeApp(firebaseConfig);
}else{
    app=firebase.app()
}

const auth = firebase.auth()

export { auth };*/